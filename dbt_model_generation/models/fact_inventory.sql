{#
------------------------------------------------------------------------
MODEL NAME    : fact_inventory
TARGET TABLE  : BSL_MA.DWH_MA.FACT_INVENTORY
SOURCE TABLES : BSL_RAW.DWH_RAW.INVENTORY
DESCRIPTION   : This model creates a fact table for inventory data with 
                latest snapshot information, including product and supplier
                lookups with proper handling of missing references
PREREQUISITES : dim_product and dim_supplier models should exist
PARAMETER     : None
AUTHOUR       : AI Generated by Shajahan - 2025-11-27
------------------------------------------------------------------------
#}

{{ config(
    materialized = 'table',
    alias = 'fact_inventory'
) }}

WITH source_data AS (
    SELECT
        inventory.product_id,
        inventory.supplier_id,
        inventory.stock_qty,
        inventory.warehouse_location,
        inventory.last_updated,
        inventory.inserted_at
    FROM {{ source('dwh_raw', 'inventory') }} AS inventory
),

transformed AS (
    SELECT
        -- Auto corrected using semantic file: product_id lookup with unknown handling
        CASE 
            WHEN source_data.product_id IS NULL THEN '0'
            ELSE source_data.product_id
        END AS product_id,
        
        -- Auto corrected using semantic file: supplier_id lookup with unknown handling  
        CASE 
            WHEN source_data.supplier_id IS NULL THEN '0'
            ELSE source_data.supplier_id
        END AS supplier_id,
        
        -- Direct mapping with negative value handling
        CASE 
            WHEN source_data.stock_qty < 0 THEN NULL
            ELSE source_data.stock_qty
        END AS stock_qty,
        
        -- Uppercase location transformation
        UPPER(source_data.warehouse_location) AS warehouse_location,
        
        -- Convert timestamp to date
        DATE(source_data.last_updated) AS snapshot_date,
        
        -- Flag latest record per product_id+supplier_id by max(last_updated)
        CASE 
            WHEN source_data.last_updated = MAX(source_data.last_updated) OVER (
                PARTITION BY source_data.product_id, source_data.supplier_id
            ) THEN TRUE
            ELSE FALSE
        END AS is_latest,
        
        -- Direct mapping for last_updated
        source_data.inserted_at AS last_updated
        
    FROM source_data
)

SELECT * FROM transformed