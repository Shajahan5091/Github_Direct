{#
------------------------------------------------------------------------
MODEL NAME    : fact_inventory
TARGET TABLE  : BSL_MA.DWH_MA.FACT_INVENTORY
SOURCE TABLES : BSL_RAW.DWH_RAW.INVENTORY
DESCRIPTION   : Inventory latest snapshot fact table with product and 
                supplier lookups, stock quantities, warehouse locations,
                and latest record flags
PREREQUISITES : dim_product and dim_supplier tables should exist
PARAMETER     : backfill_flag, backfill_start_date, backfill_end_date
AUTHOUR       : AI Generated by Shajahan - 2025-11-25
------------------------------------------------------------------------
#}

{{ config(
    materialized = 'table'
) }}

WITH source_data AS (
    SELECT
        inventory.product_id,
        inventory.supplier_id,
        inventory.stock_qty,
        inventory.warehouse_location,
        inventory.last_updated,
        inventory.inserted_at
    FROM {{ source('dwh_raw', 'inventory') }} AS inventory
    {% if var('models')['backfill_flag'] == 'true' %}
        WHERE inventory.inserted_at BETWEEN '{{ var("models")["backfill_start_date"] }}' AND '{{ var("models")["backfill_end_date"] }}'
    {% else %}
        {% if is_incremental() %}
            WHERE inventory.inserted_at > (SELECT MAX(inserted_at) FROM {{ this }})
        {% endif %}
    {% endif %}
),

transformed AS (
    SELECT
        -- Product lookup with unknown handling
        CASE 
            WHEN source_data.product_id IS NULL THEN '0' -- auto corrected using semantic file
            ELSE source_data.product_id 
        END AS product_id,
        
        -- Supplier lookup with unknown handling  
        CASE 
            WHEN source_data.supplier_id IS NULL THEN '0' -- auto corrected using semantic file
            ELSE source_data.supplier_id 
        END AS supplier_id,
        
        -- Stock quantity with negative value handling
        CASE 
            WHEN source_data.stock_qty < 0 THEN NULL
            ELSE source_data.stock_qty 
        END AS stock_qty,
        
        -- Uppercase warehouse location
        UPPER(source_data.warehouse_location) AS warehouse_location,
        
        -- Convert timestamp to date for snapshot_date
        DATE(source_data.last_updated) AS snapshot_date,
        
        -- Flag latest record per product_id + supplier_id combination
        CASE 
            WHEN source_data.last_updated = MAX(source_data.last_updated) OVER (
                PARTITION BY source_data.product_id, source_data.supplier_id
            ) THEN TRUE
            ELSE FALSE 
        END AS is_latest,
        
        -- Direct mapping for last_updated from inserted_at
        source_data.inserted_at AS last_updated
        
    FROM source_data
)

SELECT * FROM transformed