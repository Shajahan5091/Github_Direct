{#
------------------------------------------------------------------------
MODEL NAME    : dim_supplier
TARGET TABLE  : BSL_MA.DWH_MA.DIM_SUPPLIER
SOURCE TABLES : BSL_RAW.DWH_RAW.SUPPLIERS
DESCRIPTION   : Dimension table for suppliers containing supplier details
                with data transformations including name formatting, email
                validation, and country standardization
PREREQUISITES : Source table SUPPLIERS must exist in BSL_RAW.DWH_RAW schema
PARAMETER     : None
AUTHOUR       : AI Generated by Shajahan - 2025-11-25
------------------------------------------------------------------------
#}

{{ config(
    materialized = 'table'
) }}

WITH source_data AS (
    SELECT
        supplier_id,
        supplier_name,
        contact_email,
        country,
        active_flag,
        inserted_at
    FROM {{ source('dwh_raw', 'suppliers') }}
    {% if var('models')['backfill_flag'] == 'true' %}
        WHERE inserted_at BETWEEN '{{ var("models")["backfill_start_date"] }}' AND '{{ var("models")["backfill_end_date"] }}'
    {% else %}
        {% if is_incremental() %}
            WHERE inserted_at > (SELECT MAX(inserted_at) FROM {{ this }})
        {% endif %}
    {% endif %}
),

transformed AS (
    SELECT
        supplier_id,
        TRIM(INITCAP(supplier_name)) AS supplier_name, -- Trim and title-case
        CASE 
            WHEN REGEXP_LIKE(contact_email, '^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\\.[A-Za-z]{2,}$') 
            THEN LOWER(contact_email) 
            ELSE NULL 
        END AS contact_email, -- Lowercase email, auto corrected using semantic file
        UPPER(country) AS country, -- Uppercase country
        COALESCE(active_flag, FALSE) AS is_active, -- Direct mapping with null default false
        inserted_at AS last_updated, -- Direct mapping, auto corrected using semantic file
        CURRENT_TIMESTAMP() AS last_updated_timestamp -- Added for table materialization
    FROM source_data
)

SELECT * FROM transformed