{#
------------------------------------------------------------------------
MODEL NAME    : dim_address
TARGET TABLE  : bsl_ma.dwh_ma.dim_address
SOURCE TABLES : bsl_raw.dwh_raw.addresses, bsl_ma.dwh_ma.dim_customer
DESCRIPTION   : Dimension table for address data with customer lookup
                and data transformations including trimming whitespace
                and standardizing city names to uppercase
PREREQUISITES : dim_customer model must exist and be populated
PARAMETER     : None
AUTHOR        : AI Generated by Shajahan - 2025-11-19
------------------------------------------------------------------------
#}

{{ config(materialized='table') }}

with source_addresses as (
    select
        address_id,
        customer_id,
        address_line1,
        city,
        country,
        inserted_at
    from {{ source('dwh_raw', 'addresses') }}
),

customer_lookup as (
    select
        customer_id
    from {{ ref('dim_customer') }}
),

transformed_addresses as (
    select
        src_addr.address_id,
        src_addr.customer_id,
        trim(src_addr.address_line1) as address_line1,
        upper(src_addr.city) as city,
        src_addr.country,
        src_addr.inserted_at::date as inserted_at
    from source_addresses as src_addr
    inner join customer_lookup as cust_lookup
        on src_addr.customer_id = cust_lookup.customer_id
)

select
    address_id,
    customer_id,
    address_line1,
    city,
    country,
    inserted_at
from transformed_addresses